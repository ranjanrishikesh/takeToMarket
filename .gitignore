# Claude Code local settings — machine-specific, contains absolute paths
.claude/settings.local.json

# GSD build artifacts — not needed by users
.planning/

# User's private marketing data
.taketomarket/
.marketing/  # legacy folder (v2.2 and earlier) — kept ignored to protect users mid-migration

# Internal dev notes (parent dir negation requires file-level patterns)
docs/*
docs/**/
# Exception: reviewer outreach list is a public-facing launch tracker.
!docs/reviewer-outreach-list.md
